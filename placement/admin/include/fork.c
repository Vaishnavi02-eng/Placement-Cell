#include<stdio.h>
#include<conio.h>
{
    forkexample();
    int x=1;
    if(fork==0);
      printf("child has x= %d\n,++X")
    else
      printf("parent has x=%d\n,--x")

      void main();
      return 0;
}